{
  "schema": "blocks-engine/php-transformer/parity-fixture/v1",
  "name": "html-form-submit-button-class-background-survives-cascade",
  "description": "A form's native submit button whose fill comes from an author CSS class (not an inline style attribute) stays a native `<button type=\"submit\">` that keeps those classes, so the author's fill is not rewritten onto a Gutenberg `core/button` anchor that would lose submit semantics to wp-element-button.",
  "source_reference": {
    "repo": "php-transformer",
    "path": "tests/fixtures/parity/html-form-submit-button-class-background-survives-cascade.json",
    "notes": "Regression fixture for the button-background-cascade defect: a class-owned flat background-color on a form submit control was unconditionally excluded from native block style attrs (StyleResolver::classOwnedBackgroundPaintDeclarations), so nothing but a secondary CSS-cascade repair stood between the author's fill and the theme's default button background. See #1894/#1896."
  },
  "legacy_comparison": {
    "skip": true,
    "reason": "Native button color-support fidelity for form submit controls is a PHP transformer behavior with no legacy equivalent."
  },
  "operation": "html_transformer.transform",
  "input": {
    "content": "<form><button type=\"submit\" class=\"btn btn-fill\">Send Message</button></form>",
    "options": { "static_css": ".btn{padding:12px 24px;border-radius:6px}.btn-fill{background-color:#13314f;color:#ffffff}" }
  },
  "expected_blocks": [
    { "path": "blocks.0.innerBlocks.0", "name": "custom/authored-button", "attrs": { "type": "submit", "className": "btn btn-fill", "text": "Send Message" } }
  ],
  "expected_fallbacks": [],
  "expect": [
    { "path": "status", "assert": "equals", "value": "success" },
    { "path": "serialized_blocks", "assert": "contains", "value": "<button type=\"submit\" class=\"btn btn-fill\">Send Message</button>" },
    { "path": "serialized_blocks", "assert": "not_contains", "value": "wp-element-button" },
    { "path": "fallbacks", "assert": "count", "count": 0 }
  ]
}

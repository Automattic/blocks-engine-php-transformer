{
  "schema": "blocks-engine/php-transformer/parity-fixture/v1",
  "name": "mixed-source-markdown",
  "description": "Converts a nested Markdown source document shape from a mixed-source product fixture.",
  "source_reference": {
    "repo": "php-transformer",
    "path": "tests/fixtures/mixed-source-site/content/story.md",
    "notes": "Covers the Markdown document body path only; products own source discovery, route maps, and skipped diagnostics."
  },
  "legacy_comparison": {
    "skip": true,
    "reason": "Mixed-source import report and link-rewrite assertions require the consuming product's WordPress runtime."
  },
  "operation": "format_bridge.convert",
  "input": {
    "from": "markdown",
    "to": "html",
    "content": "# Markdown Story\n\nThis page came from a nested Markdown source document.\n\n[Read the notes](notes.markdown)\n"
  },
  "expect": [
    { "path": "converted", "assert": "contains", "value": "<h1>Markdown Story</h1>" },
    { "path": "converted", "assert": "contains", "value": "href=\"notes.markdown\"" },
    { "path": "blocks", "assert": "count", "count": 3 },
    { "path": "blocks.0.blockName", "assert": "equals", "value": "core/heading" },
    { "path": "blocks.2.innerHTML", "assert": "contains", "value": "href=\"notes.markdown\"" },
    { "path": "blocks.2.attrs.content", "assert": "equals", "value": null },
    { "path": "supported_formats", "assert": "equals", "value": ["blocks", "html", "markdown"] }
  ]
}
